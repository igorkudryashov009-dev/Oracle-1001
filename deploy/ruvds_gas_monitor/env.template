# Managed by deploy/ruvds_gas_monitor — secrets ONLY on Rucloud (private runner).
# chmod 600. Never copy this file to the London (LD8) public vitrine host.

PROVIDER_NAME=vesselfinder
PROVIDER_BASE_URL=https://api.vesselfinder.com/vessels
PROVIDER_API_KEY=YOUR_PROVIDER_API_KEY_HERE
PROVIDER_COST_PER_CALL_USD=0.05
PROVIDER_MONTHLY_BUDGET_USD=50

# Paths relative to WorkingDirectory=/opt/oracle1001/weekly_monitor
FLEET_CSV=/opt/oracle1001/weekly_monitor/data/fleet_database.csv
GAS_MONITOR_OUT_DIR=/opt/oracle1001/weekly_monitor/features/gas_carrier_weekly

# Bootstrap only: set to 1 to allow --dry-run when API key is still a placeholder.
# Set to 0 (and put a real PROVIDER_API_KEY) for production credit spend.
GAS_MONITOR_ALLOW_DRY_RUN=1
